repos:
  - repo: local
    hooks:
      - id: pytest
        name: Run test suite with pytest
        entry: poetry run pytest -q
        language: system
        pass_filenames: false
        stages: [commit]
